// The name of the shard this dbserver manages
ShardName Ouroboros

//// ---------------- ////

// Override auto-detected MapServer IP
AdvertisedIp 127.0.0.1
// Connects DBServer to this AuthServer
AuthServer 127.0.0.1 2104
// Not yet available in Volume 2
BeaconMasterServer 127.0.0.1
// Connects DBServer to this ChatServer
ChatServer 127.0.0.1
// Connects supplementary servers to this DBServer
DbServer 127.0.0.1
// Used to identify an IP address as local and allow it to connect. In a MapServer context it skips this interface and uses the next IP available on the system
LocalIp 127.0.0.1
// Connects DBServer to this LogServer
//LogServer 127.0.0.1

//// ---------------- ////

// The locale of the shard
// 0/en: English
// 1/test: Test Locale
// 2/zh: Traditional Chinese
// 3/ko: Korean
// 4/ja: Japanese
// 5/de: German
// 6/fr: French
// 7/es: Spanish
Locale en

//// ---------------- ////

// XP Experience multiplier
XPScale 1.0
// AE Experience and Influence multiplier
AEScale 1.0
// AE Ticket multiplier
TicketScale 1.0

//// ---------------- ////

// The SQL database type. Only mssql is supported right now
SqlDbProvider mssql
// The name of the dbserver database
SqlDbName cohdb
// An SQL query to initialize the dbserver database for the first time
SqlInit "create database cohdb;"
// Connection string. By default uses Windows mode auth (run under an unprivileged user!)
SqlLogin "Driver={ODBC Driver 17 for SQL Server};Server=localhost;Persist Security Info=False;Trusted_Connection=yes;"
//SqlLogin "Driver={ODBC Driver 17 for SQL Server};Server=localhost;Uid=sa;Pwd=YOUR_DB_PASSWORD;"
// Enable or disable the use of DDL for manipulating the database. This is the master switch for all of the related SQL directives. DDL must be enabled in order to perform many operations for altering the database schema. If a required DDL command is not allowed, the server will exit
SqlAllowDDL 1
// Allow the SqlAddAttributes DDL command
SqlAddAttributes 1
// Allow the SqlAddColumnOrTable DDL command
SqlAddColumnOrTable 1
// Allow the SqlDeleteColumnOrTable DDL command
SqlDeleteColumnOrTable 1
// Allow the SqlRebuildTable DDL command
SqlRebuildTable 1
// Allow the SqlAlterColumn DDL command
SqlAlterColumn 1
// Runs some stored procedure
//ChangeDbOwnerFrom username

//// ---------------- ////

// Enable fake auth. This lets clients log in directly to the dbserver (with any account) instead of going via an authserver
UseFakeAuth 0
// Set the access level of new characters
//DefaultAccessLevel 0
// Set the log file directory. Default is logs inside the game data directory
//LogDir "logs"

// Set the behavior of assertion failures. Multiple modes can be specified if the argument is wrapped in quotes
// Minidump, Fulldump, Exit, NoTimestamp, NoDate, Zip, Ignore
AssertMode Fulldump

// The maximum number of players that can be connected at any time. The maximum value is AUTH_SIZE_MAX (8191)
MaxPlayers 100
// Number of players that the queueserver is allowed to admit per minute. Default is 100
//LoginsPerMinute 100
// DEPRECATED
MinPlayers 0
// Enable or disable the use of the queueserver
UseQueueServer 1
// Put F2P players in the queue if the account server is down
BlockFreePlayersIfNoAccountServer 0
// Let people into the shard even if we would queue them normally
//EnqueueWithAuthLimiter 0
// Disable the statserver -- have the launcher send -nostats to mapservers
NoStats 0
// Set the "registry name". This is used to construct the path to the Windows registry keys
ClientProject coh
// Extra parameters to send to launched mapservers
MapServerParams "-productionmode"
// Possibly perform extra verification checks on containers? Sends -diffdebug to launched mapservers
//DiffDebug 1
// Possibly perform extra verification checks on containers?
//ContainerSizeDebug 1
// Overridden by LogServer
UseLogServer 0
// Enable verbose logging
//LogRelayVerbose 1
// Global max level for NPCs
MaxLevel 50
// NPC max level for blue zones
MaxCoHLevel 50
// NPC max level for red zones
MaxCoVLevel 50
// Number of shared H/V character slots per player, range is 0-48
//MaxDualSlots 12
// Number of days to keep stored backups for. Minimum is 30 days
//BackupDays 30
// Highest level of character that can be moved to offline storage. Default is 35
//OfflineProtectLevel 35
// Number of days before a character is moved to offline storage
//OfflineIdleDays 7
// Sends -completebrokentasks to launched mapservers
//CompleteBrokenTasks 0
// Log when packets are dropped or received out of order
//DroppedPacketLogging 0
// Extra verification on auth packets
//AuthBadPacketReconnect 1
// Don't launch the master beaconserver
DoNotLaunchBeaconMasterServer 0
// Number of beaconrequestservers to launch. Negative values correspond to 2
BeaconRequestServerCount 1
// Number of beaconclients to launch. Minimum 2 (sentry+worker) and maximum [CPU cores] + 1
BeaconClientCount 2
// Don't preload the crash report DLL (CrashRpt.dll)
DoNotPreloadCrashReportDLL 1
// Cache directory for requests made to the beaconservers
BeaconRequestCacheDir "beaconizer"
// Don't launch any beaconclients
DoNotLaunchBeaconClients 0
// Don't launch any TSR mapservers. TSR mapservers preload data to shared memory to make loading other mapservers faster
DoNotLaunchMapServerTSR 0
// Difference in hours between the time on the mapserver and the dbserver
//RaidTimeZoneDelta 0
// Disable on-disk container backups
//DisableContainerBackups 1
// Auction entries for accounts that have not logged in in this many days are not sent
AuctionInvMaxLastLoginDays 60
// Associated with BlockFreePlayersIfNoAccountServer and EnqueueWithAuthLimiter
//AuthnameLimiterEnabled 0
// Characters greater than this access level can bypass the auth limiter
//AuthnameLimiterAccessLevel 20
// A comma delimited list of map keys to not load. 
//BlockedMapKey 0
// A comma delimited list of disabled zone events. 
//DisabledZoneEvents 0
// A list of commands to be executed by the client upon connection
//ClientCommands ""
// The maximum amount of bytes allowed in the send queue to the missionserver
//MissionserverMaxSendQueueSize 1000000
// The maximum amount of bytes allowed in the publish queue to the missionserver
//MissionserverMaxSendQueuePublishSize 500000

// Force user authbits to be set regardless of the state in the database. The format is comma or space-separated authbit names. Possible options (can be combined):
// Con2009Bit5, Con2009Bit4, Con2009Bit3, Con2009Bit2, Con2009Bit1, MagicPack, SupersciencePack, MartialArtsPack,
// RogueAccess, GRPreOrderWillOfTheSeers, GRPreOrderClockworkEfficiency, GRPreOrderMightOfTheEmpire, GRPreOrderResistanceTactics,
// RogueCompleteBox, LoyaltyReward, CoralaxBossCostume, VahzilokMeatDocCostume, RiktiCostume, PraetoriaPDCostume, GhoulCostume,
// MutantPack, MonthsPlayed, RecreationPack, HolidayPack, AuraPack, VanguardPack, Facepalm, AnimalPack, LimitedRogueAccess,
// GRPreOrderSyndicateTechniques, CyborgPack, MacPack, IsRetailAccount, TargetPromo, PaxPromo, GenConPromo, ComiconPromo, ArachnosAccess,
// WeddingPack, TrialAccount, PocketDVIP, JumpPack, JetPack, KoreanLevelMinimum, Valentine2006, CovBeta, CovPreorder1, CovPreorder2,
// CovPreorder3, CovPreorder4, CovSpecialEdition, VillainAccess, DVDSpecialEdition, Kheldian, Beta, TransferToEU, HeroAccess
//OverrideAuthBit RogueAccess

// Whether or not this shard is a beta shard. On beta shards, users can use the broken mission fix every 10 minutes, rather than every 3 days
//IsBetaShard 0
// Whether or not this shard is a VIP shard. Only accounts marked as VIP can log into this shard
//IsVIPShard 0
// Show the nag dialogs and buy button for Going Rogue
//GoingRogueNagAndPurchase 0
// How many days of past karma event history to read on load. Default is 30
//KarmaEventHistoryDays 30
// Number of seconds to keep character names temporarily reserved when performing a name lookup. This must be greater than the client refresh time, which is 600
//NameLockTimeout 1200

//// ---------------- ////

// LogLevel values are -2 to 3, increasing in verbosity every level
// -2 -- DISABLED// do not log anything
// -1 -- ALERT// this is so important that someone should be notified
//  0 -- IMPORTANT// only the most important information. Default value
//  1 -- VERBOSE// information that we would like, but has the possibility of spamming the logs with too much data
//  2 -- DEPRECATED// there are old things that are doubtful we should even be logging
//  3 -- DEBUG// lots of information for debugging only

// Crash reports
//SetLogLevel "crashDetails" -2
// Logs for debugging purposes. Usually temporary, while the bug is being tracked down
//SetLogLevel "debug" -2
// Logs for the text journaling system
//SetLogLevel "persist" -2
// Errors we want to know about sent here
//SetLogLevel "error" -2
// Log messages that are probably worthless are sent here, on the off-chance we still need them
//SetLogLevel "old" -2
// Used to generate output from test clients
//SetLogLevel "TestClient" -2
//SetLogLevel "packets" -2

// DBServer
//SetLogLevel "delete" -2
//SetLogLevel "offline" -2
SetLogLevel "SystemSpecs" 1
//SetLogLevel "OverloadProtection" -2
//SetLogLevel "CharSlotApply" -2

// MapServer
// General log of things on entities (no chat, powers, or rewards related)
//SetLogLevel "entity" -2
// Power Info (may be redundant since chat records lots of powers information)
//SetLogLevel "powers" -2
// Supergroup-specific things
//SetLogLevel "supergroup" -2
// Player reported bugs
//SetLogLevel "bug" -2
SetLogLevel "chat" 1
// Egregious cheats sent here
//SetLogLevel "cheaters" -2
// Anything to do with player inventory should go here
//SetLogLevel "rewards" -2
// Scripted zone event reward logs
//SetLogLevel "sze_rewards" -2
//SetLogLevel "survey" -2
//SetLogLevel "cmds" -2
// Logs admin commands
SetLogLevel "internalcmds" 1
//SetLogLevel "MARTY" -2
//SetLogLevel "Admin" -2
//SetLogLevel "Performance" -2
//SetLogLevel "DuplicateMessageIDs" -2

// AccountServer
// Generic account log
//SetLogLevel "Account" -2
// Account transaction log
//SetLogLevel "Transaction" -2

// ArenaServer
// In depth arena event logs
//SetLogLevel "ArenaEvent" -2

// AuctionServer
//SetLogLevel "Auction" -2
//SetLogLevel "Xaction" -2

// MissionServer
//SetLogLevel "DeletedArcs" -2
//SetLogLevel "missionserver" -2

// RaidServer
//SetLogLevel "iop" -2

// StatServer
//SetLogLevel "statserver" -2

// TurnstileServer
//SetLogLevel "turnstile" -2

//// ---------------- ////

// Propagates the configured loglevels to all of the connected servers
//IsLoggingMaster 1
// Enable/disable the MARTY anti-cheat
MARTYEnabled 1
// When sending the list of characters to a client, delay the SQL query by this many milliseconds
//DebugSendPlayersDelayMS 1000
// Number of loyalty points automatically granted to FakeAuth accounts
//DefaultLoyaltyPointsFakeAuth 46
//DefaultLoyaltyLegacyPointsFakeAuth 46
// Set/unset the Going Rogue authbit. Only works on FakeAuth shards
//OwnsGoingRogue 1

// Forces overload protection to be enabled
//ForceOverloadProtection 1
// Launcher upper bound for overload protection. When the percentage of launchers that cannot launch a server rises above this percentage, engage overload protection
//OverloadProtection_LauncherHighWaterMarkPercent 0.5
// Launcher lower bound for overload protection. When the percentage of launchers that cannot launch a server drops below this percentage, disengage overload protection
//OverloadProtection_LauncherLowWaterMarkPercent 0.5
// SQL upper bound for overload protection. When the size of the SQL queue grows larger than this, engage overload protection
//OverloadProtection_SQLQueueHighWaterMark 10000
// SQL lower bound for overload protection. When the size of the SQL queue drops below this, disengage overload protection   
//OverloadProtection_SQLQueueLowWaterMark 9000

// Shut down mapservers if they are idle for this many minutes. Default is 0 (idle shutdown is disabled). Keep in mind that maps without Transient in their maps.db entry will never shut down, even after being idle
MapserverIdleExit 30
// Number of minutes a mapserver must be idle before performing idle maintenance. Idle maintenance will attempt to minimize the memory footprint of the mapserver by compacting the working set
MapserverIdleUpkeep 30
// Specify an hour range (24-hour clock) for when to perform daily maintenance on static mapservers. Daily maintenance is disabled by default
//MapserverDailyUpkeep 4 5
// Whether door updates should be sent to all mapservers. This is a legacy behavior// by default, updates are only sent to static maps and supergroup bases, and not mission maps, raid maps and arena maps
//SendDoorsToAllMaps 1
